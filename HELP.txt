1. треба отримати дані рандомно з бази
	1,1.треба створити маршрутизатор, в якому має бути прописана логіка ддля отримання даних
(,-_-,)
2. вивести одне з них
3. перевірити

const getRandowWord = async((req, res)=>{
    try{
    const allWords = await WorModel.find()

    const randomIndex = Mathfloor(Mathranndom()*allWords.lenght)
    const randomWord = allWords[randomIndex]
    
    res.status(200).json(randomWord)
    }catch(error){
        res.status(400).send(error)
    }
})